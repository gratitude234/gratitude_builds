export function Footer() {
  return (
    <footer className="mt-16 border-t border-emerald-900/10">
      <div className="site-shell flex flex-col gap-2 py-8 text-sm text-muted sm:flex-row sm:items-center sm:justify-between">
        <p>gratitude-builds</p>
        <p>Building in public. One day at a time.</p>
      </div>
    </footer>
  );
}
